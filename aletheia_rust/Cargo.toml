[package]
name = "aletheia_rust"
version = "0.2.0"
edition = "2021"

[lib]
name = "aletheia_rust"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
