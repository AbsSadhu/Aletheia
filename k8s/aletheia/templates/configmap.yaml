apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aletheia.fullname" . }}-config
  labels:
    {{- include "aletheia.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
